# NepalKamma — Web client

Next.js + TypeScript web client for NepalKamma — a local gig marketplace connecting talent and jobs.

This README explains how to run the project locally, important implementation notes, and common troubleshooting steps.

## Quick overview

- Frontend: Next.js (App Router), React (Client + Server components), TypeScript, Tailwind CSS
- State: SWR (server canonical) + Zustand (app-wide store)
- Realtime: WebSockets (socket hooks) for chat and presence
- HTTP client: axios, custom `fetcher` for SWR

## Prerequisites

- Node.js (v18+ recommended)
- pnpm / npm installed
- Backend server running (the frontend expects an API and socket server)

## Getting started (development)

1. Clone the repo

```bash
git clone <repo-url> NepalKammaWeb
cd NepalKammaWeb/web
```

2. Install dependencies

```bash
# using npm
npm ci

# or using pnpm
pnpm install
```

3. Create environment file

Create `.env.local` in the `web/` folder with values used by the app. Example:

```env
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your_key
```

The backend will need its own env (database, cloudinary, JWT secret, etc.).

4. Start the backend

Make sure your backend is running and exposes a lightweight health endpoint (recommended `/health`) that returns HTTP 200.

5. Start the frontend

```bash
npm run dev
# open http://localhost:3000
```

## Useful scripts

- `npm run dev` — run Next.js dev server
- `npm run build` — build for production
- `npm run start` — start production server
- `npm run lint` — lint the project

## Important implementation notes

- Authentication: `useAuth` hook (SWR) fetches `/auth/check-auth` and writes the user into the Zustand store. When you update user data on the server, call SWR `mutate()` for that key or call a `fetchUser()` in the store to keep UI in sync.
- Document verification: After upload, backend should return the updated user object, or the frontend should revalidate the user. Otherwise the UI will show stale data until a reload.
- Offline & server detection: Prefer using SWR to detect server health (`/health`) rather than manual intervals. SWR avoids memory leaks and deduplicates requests.
- reCAPTCHA: The widget challenge popup is controlled by Google. For mobile, use the `compact` size or consider Invisible reCAPTCHA / v3 to avoid puzzle popups.

## Tailwind & custom breakpoints

If you need custom breakpoints (for example to hide a mobile sidebar at 1240px) add a `tailwind.config.js` at repo root:

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/app/**/*.{ts,tsx,js,jsx}', './src/components/**/*.{ts,tsx,js,jsx}'],
  theme: {
    extend: {
      screens: { desktop: '1240px' }
    }
  }
}
```

Restart the dev server after editing `tailwind.config.js`.

## Debugging tips

- 404 on route: ensure the file exists under `src/app/<route>/page.tsx` (App Router). For a global 404 create `src/app/not-found.tsx`.
- Hydration mismatch: guard client-only logic with `useEffect` or render nothing until `mounted` to avoid server/client HTML differences.
- Stale user after update: call `mutate('/auth/check-auth')` or `fetchUser()` from Zustand after server-side updates.
- Server unreachable banner not appearing: ensure SWR call uses a fetcher and a health endpoint exists on the API.

## Contributing

- Follow repo lint rules and add tests where appropriate
- Keep PRs focused and include reproduction steps for bugs

## Optional follow-ups I can add

- `src/app/not-found.tsx` scaffold (404 page) matched to your design
- `tailwind.config.js` with the `desktop` breakpoint
- `.env.example` with required keys

If you want any of those added automatically, tell me which and I will add them.

---

Generated by assistant — lightweight README covering setup and common pitfalls for this project.
